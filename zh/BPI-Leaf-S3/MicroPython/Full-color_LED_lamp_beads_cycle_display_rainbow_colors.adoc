TIP: link:../GettingStarted_BPI-Leaf-S3[返回开始使用 BPI-Leaf-S3]


在上一节的基础上，我们可以更进一步，写一个循环来自动改变灯珠的颜色。

```py
from machine import Pin
from neopixel import NeoPixel
import time

def rainbow(num=1,level=25,delay=100):
     def write_all(num,delay,red,green,blue):
         for j in range (num):
             np[j] = (red,green,blue)
         np. write()
         time. sleep_ms(delay)
    
     red,green,blue = level,0,0
    
     rainbow_step_list2 = [(0,1,0),(-1,0,0),(0,0,1),(0,-1,0),(1,0,0),(0,0, -1)]
    
     for step in rainbow_step_list2:
         for i in range (level):
             red+=step[0]
             green+=step[1]
             blue+=step[2]
             write_all(num,delay,red,green,blue)
            


np = NeoPixel(Pin(48, Pin.OUT), 1,bpp=3, timing=1)

while True:
     rainbow(num=1,level=25,delay=10)

```
本例程可适用于任意长度的ws2812灯带。

修改第一个参数 `NeoPixel(Pin(48, Pin.OUT), 1,bpp=3, timing=1)` 对任何 GPIO 灯带串联引脚。

修改num参数 `rainbow(num=1,level=25,delay=100)` 为灯带上对应灯珠的数量。

当然，我们也可以根据自己的想法，使用for循环或者while循环来做出我们想要的颜色变化图案。
