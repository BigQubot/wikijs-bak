TIP: link:/zh/BPI-Bit/Bit_for_MicroPython#_板卡开发工具[BPI-Bit 的 MicroPython 教程]


= mpfshell 使用说明

在 Python 3.5 或更高版本中，可使用以下命令进行安装：
```sh
pip install mpfshell-lite 
```
安装完成后，输入以下命令：
```sh
mpfshell-lite
```
即可启动 mpfshell。

image::/bpi-bit/mpfshell_install.png[mpfshell_install.png]
