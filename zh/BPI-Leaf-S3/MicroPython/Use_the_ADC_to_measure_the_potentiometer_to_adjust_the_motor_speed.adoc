TIP: link:../GettingStarted_BPI-Leaf-S3[返回开始使用 BPI-Leaf-S3]

== 外部硬件要求

* 电位器×1
* TB6612FNG 电机驱动模块 x 1
* 5V直流电机×1
* 一些连接线

== 连接方式

[options="header"]
|============================
| 电位器 | BPI-Leaf-S3
| 接地           | 接地        
| 电压控制电路           | 3V3        
| S             | 14         
|============================

[options="header"]
|========================
| TB6612FNG| BPI-Leaf-S3
| 脉宽调变      | 11         
| AIN2      | 13         
| AIN1      | 12         
| 待机      | 10         
| 虚拟机        | 5V         
| 电压控制电路       | 3.3V       
| 接地       | 接地        
|========================

[options="header"]
|=========================
| TB6612FNG| 电机       
| AO1       | 电机N极
| AO2       | 电机S极
|=========================

== 运行结果

开发板会输出读取到的电压值 ADC 在 REPL 间隔100ms，单位mv，以及相应的受控 PWM 占空比。

用手调节电位器来改变其输出电压。电压越高，电压越高 PWM 占空比由开发板输出，电机转速越快。

https://www.youtube.com/watch?v=2_UeeeOBJwo[YouTube 视频]

== 代码
```py
from machine import Pin,ADC,PWM
import time

adc14 = ADC(Pin(14),atten=ADC.ATTN_11DB)

PWM_A = PWM(Pin(11)) #Set PWM output pin
PWM_A.freq(20000) #Set PWM frequency
PWM_A. duty(0) #Set PWM duty cycle
AIN1 = Pin(12,Pin.OUT)
AIN2 = Pin(13,Pin.OUT)
STBY = Pin(10,Pin.OUT)
AIN1.on() #MOTOR forward
AIN2.off()
STBY.on() #When STBY pin is at high level, TB6612FNG starts.

while True:
     read_mv=adc14.read_uv()//1000
     if read_mv <= 3000:
         duty_set = int(1023/3000 * read_mv)
     else:
         duty_set = 1023
     PWM_A. duty(duty_set)
     Duty_cycle = int(duty_set/1023*100)
     print("ADC_read={0}mv,Duty_cycle={1}%".format(read_mv,Duty_cycle))
     time. sleep_ms(100)

```