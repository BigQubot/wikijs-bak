= 简介

== 第一步：下载并编译 BSP 代码
这里我使用 BPI-M4 举个例子：

. 下载 BSP 代码并执行：
+
```sh
git clone https://github.com/BPI-SINOVOIP/BPI-M4-bsp.git
```

. 构建 BSP，执行：
+
```sh
./build.sh
```
然后选择“**1**”。

+
image::/picture/build_image_1.png[build_image_1.png]

. BSP 代码构建完成后，会在“**SD/bpi-m4**”中生成引导文件和内核文件。
+
image::/picture/build_image_2.png[build_image_2.png]

== 第二步：将镜像烧录到 SD 卡
. 可参考“在 Banana Pi 单板计算机上安装 Linux 和 Android 镜像”的说明烧录镜像。

. 本例在 Ubuntu 系统中烧录镜像。
+
```sh
bpi-copy 2019-07-23-ubuntu-18.04-mate-desktop-preview-bpi-w2-m4-sd-emmc.img.zip /dev/sde
```
+
image::/picture/build_image_3.png[build_image_3.png]

== 第三步：制作镜像
. BSP 无法直接生成完整镜像，因此需要从已烧录系统的 SD 卡制作镜像。

. 将刚刚烧录镜像的 SD 卡插入 Ubuntu 计算机，系统会识别“**BPI-BOOT**”和“**BPI-ROOT**”两个分区。

. 制作镜像前先完成以下准备：
+
```sh
cd /media
ln -s <your account> pi
mkdir <directory>
cp <BPI-M4-Project>/SD/bpi-m4/*.tgz <BPI-M4-Project>/SD/bpi-m4/100MB/* image
```

. 如上所示，系统在“**/media**”下挂载了名为“**pi**”的用户目录；随后创建文件夹，并将所有 BSP 文件复制到其中。

. 现在我们可以使用命令
+
```sh
bpi-migrate -c bpi-w2-m4.conf -c ubuntu-18.04-mate-from-sd.conf
```
+
image::/picture/build_image_4.png[build_image_4.png]

. 此命令会从 SD 卡复制操作系统，并在新镜像中替换为刚刚构建的内核和 U-Boot。

== 注意事项
WARNING: 如何查找开发板的 .conf 文件？

您可以在这里看到这些文件： https://github.com/BPI-SINOVOIP/BPI-files/tree/69594603ab8ceb102d77818982897c00c86d6d15/others/for-bpi-tools/conf/board

WARNING: 如何查找操作系统的 .conf 文件？

您可以在这里看到这些文件： https://github.com/BPI-SINOVOIP/BPI-files/tree/c2f1fcaf34cf8e5b58b1e0093c7597b0d31ca0f6/others/for-bpi-tools/conf/os
