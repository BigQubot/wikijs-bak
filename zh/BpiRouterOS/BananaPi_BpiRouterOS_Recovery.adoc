# BpiRouter OS — 恢复与救砖


> BpiRouter OS 的核心可靠性能力：从「改错配置」到「整机变砖」，逐级都有救回手段。**刷坏也救得回，不会永久变砖。**

[← link:/zh/BpiRouterOS/BananaPi_BpiRouterOS[返回首页]]

---

## 0. 先认准你的「恢复键」

不同板用来恢复的按键不同（这一点很关键）：

[options="header",cols="2,4"]
|====
| 设备 | 恢复键 
| **BPI-R4** | **WPS 键**（R4 的 reset 键是硬复位线、软件不可见，所以恢复用 WPS） 
| **BPI-R4 Lite** | 物理 **reset** 键 
| **BPI-R4 Pro** | 物理 **reset** 键 
| **BPI-R3** | 物理 **reset** 键 
| **BPI-R3 Mini** | 物理 **reset** 键 
| **OpenWrt One** | 后部 **reset** 键 
|====
> 下文说的「恢复键」= 你这块板对应的那一个。

---

## 1. 四层恢复阶梯

从最轻（不丢任何东西）到最重（整机回出厂），逐级兜底。**通常你只会用到 L3。**

### L1 · 配置自动回滚（自动，无需操作）
当你修改网络 / WiFi / 防火墙等可能切断管理连接的设置时，界面顶部会出现**确认横幅 + 倒计时**：
- 改动正常、你仍能访问 → 点「保留变更」让它生效；
- 改错导致掉线、你没点确认 → **倒计时到点自动恢复**到改动前的配置。

这层保护默认开启，让你「改错也不会把自己锁在外面」。

### L2 · 固件自动回退（自动，无需操作）
固件升级用 A/B 双槽：新固件若启动失败，**引导程序会自动切回上一个好固件槽**。你无需任何操作，设备自己回到能用的版本。

### L3 · 恢复出厂（运行中长按恢复键约 5 秒）
设备能正常运行、但**配置乱了 / 忘了密码**时：

1. 在设备运行状态下，**按住恢复键约 5 秒**后松手。
2. 设备清除当前配置、重启回到出厂状态（**不重刷固件**）。
3. 重新打开 `https://192.168.100.1` 走 [link:/zh/BpiRouterOS/BananaPi_BpiRouterOS_Setup[首次配置]]。

> 仅清当前固件槽的配置，不动另一个槽、不重刷固件。

### L4 · 整机救砖（断电后长按恢复键开机）
**配置和固件都坏了 / 设备起不来**时的终极兜底：

1. **断电。**
2. **按住恢复键不放**，给设备上电，**全程持续按住**。
3. 设备从内置黄金镜像把整机刷回出厂（接串口能看到 `*** GOLDEN RESTORE` 之类提示）。
4. 完成后自动重启，回到纯出厂状态。

> ⚠️ **要持续按住跨过开机的自检/倒计时**，直到看到救砖开始的提示再松手；松手太早会被当作正常开机。

> 💡 R4-Lite 等带独立 NOR+NAND 的板，救砖前可能需要把板上的 NAND/NOR 拨码拨到 NAND——详见随固件附带的工厂烧录说明。

---

## 2. 怎么选用哪一层？

[options="header",cols="4,5"]
|====
| 情况 | 用哪层 
| 改了个设置就连不上了 | L1（等倒计时自动回滚，或下次进去手动回退） 
| 升级后新固件有问题 | L2 自动回退；或在系统页点「回退到上一版本」 
| 忘了管理密码 / 配置乱了，但设备还能开机 | **L3**：运行中长按恢复键 5 秒 
| 设备起不来 / 刷坏了 / 反复重启 | **L4**：断电→按住恢复键→上电 
|====
---

## 3. 常见问题

**长按 reset 没反应？（R4）**

R4 的 reset 键是硬复位线、软件读不到——**R4 的恢复键是 WPS 键**，请改用 WPS。其余板用物理 reset。

**L4 按了没进救砖？**

多半是松手太早。务必**持续按住跨过开机倒计时**，直到看到救砖提示。

**救砖会丢配置吗？**

会——L4 是回到纯出厂状态。日常的配置丢失风险由 L1（自动回滚）和「升级保留配置」覆盖；L4 是最后兜底。建议平时用系统页的「下载备份」留一份配置，恢复后上传即可还原。

---

[← link:/zh/BpiRouterOS/BananaPi_BpiRouterOS[返回首页]] ·  [link:/zh/BpiRouterOS/BananaPi_BpiRouterOS_Flashing[烧录指南] →] · [link:/zh/BpiRouterOS/BananaPi_BpiRouterOS_Setup[首次配置] →]

