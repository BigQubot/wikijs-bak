= 简介

Banana Pi BPI-M4 采用 Realtek RTD1395 芯片，是一款搭载 64 位四核 Cortex-A53 处理器的迷你单板计算机。它配备 1 GB 内存和 8 GB eMMC，并板载支持 b/g/n/ac 的 Wi-Fi 与 Bluetooth 4.2。接口包括 4 个 USB 2.0、1 个 USB Type-C、1 个 HDMI 和 1 个音频插孔，还支持 M.2 Key E PCIe 2.0 接口。

TIP: 更多信息：link:/en/BPI-M4/BananaPi_BPI-M4[Banana Pi BPI-M4]

== 规格参数

- Realtek RTD1395 ARM Cortex-A53 四核 64 位
- Mali 470 MP4 GPU，支持 OpenGL ES 1.1/2.0、H.264、H.265 和 2K/4K
- 板载 1 GB DDR4（可选 2 GB）
- 8 GB eMMC 闪存（最高 64 GB）
- 10/100 Mbit/s 以太网；RTL8821 模块支持 Wi-Fi 802.11 b/g/n/ac 和 Bluetooth 4.2
- USB 2.0 Type-C
- M.2 Key E 插槽，支持 PCIe 2.0 和 USB 2.0
- 支持 PoE 功能

= 开发
== Android
=== 准备

. BPI-M4 Android 8 镜像仅支持 2 GB DDR 版本的开发板。
. BPI-M4 USB 烧录工具用于将 Android 镜像写入开发板的 eMMC，仅支持 Windows 平台。
. 确保 SW2（位于开发板底部、靠近 SD 卡插槽）已切换到 1。
. 烧录镜像前，请先阅读 USB 烧录工具包中的 document/bananapi_m4_usb_download_tool_guide.doc。

下载 link:https://download.banana-pi.dev/d/3ebbfa04265d4dddb81b/files/?p=%2FTools%2Fimage_download_tools%2Fm4_android_usb_download_tool.zip[BPI-M4 USB 烧录工具]

=== 安装镜像到 eMMC

. xxx
. xxx
. xxx
. xxx

== Linux
=== 准备

. 将开发板的 SW2（位于开发板底部、靠近 SD 卡插槽）切换到 0。
. 为使新的 Linux 镜像同时兼容 2 GB 和 1 GB 版本，旧批次（2020 年 5 月以前）的 1 GB 开发板必须更新 SPI ROM 固件，之后才能运行新版镜像。
. 登录用户名/密码：pi/bananapi 或 root/bananapi

=== 使用 xxx 安装镜像

. xxx
. xxx
. xxx
. xxx

=== 使用 xxx 安装镜像

. xxx
. xxx
. xxx
. xxx

=== 启用 SPI
由于缺少设备树 Overlay 支持，而且 40 针排针的 SPI 引脚与 UART2 复用，SPI 功能默认关闭，可参考以下主题启用：

https://forum.banana-pi.org/t/banana-pi-bpi-m4-how-to-get-i2c-and-spi-running-tutorial/12824
也可以使用 fdtput 命令启用，无需重新编译内核。操作前请确保已经挂载启动分区。
```sh
 # fdtput -t s /media/pi/BPI-BOOT/bananapi/bpi-m4/linux/rtd-1395-bananapi-m4-2GB.dtb /spi@9801BD00 status okay
 # fdtput -t s /media/pi/BPI-BOOT/bananapi/bpi-m4/linux/rtd-1395-bananapi-m4-1GB.dtb /spi@9801BD00 status okay
 # reboot
```

=== 检查 Docker
运行以下命令，测试镜像中是否已安装 Docker：
```sh
sudo docker run --rm hello-world
```
=== EMAC
使用 iperf3 测试 GMAC。

. 在电脑终端中执行：
+
```sh
iperf3 -s
```
. 在 BPI-M4 控制台中执行：

- TCP 测试
+
```sh
iperf3 -c serverIP
```
- UDP 测试
+
```sh
iperf3 -u -c serverIP
```
+
image::/picture/m4_emac_test.jpg[m4_emac_test.jpg]

=== 蓝牙
**可通过两种方式设置 Bluetooth**

. 使用命令设置 Bluetooth

- 使用 bluetoothctl 工具操作 Bluetooth
- 执行 “**bluetoothctl**”
- 如果不了解 bluetoothctl 的用法，请输入 “**help**” 查看更多命令
- 执行这些命令：
+
image::/picture/m4_bluetooth.jpg[m4_bluetooth.jpg]

. 使用图形界面设置 Bluetooth

=== Wi-Fi 客户端
**可通过两种方式设置 Wi-Fi 客户端**

. 使用命令设置 Wi-Fi 客户端
+
```sh
ip link set wlan0 up
iw dev wlan0 scan | grep SSID
vim /etc/wpa_supplicant/wpa_supplicant.conf
```
+
```sh
 network={    
 ssid="ssid"    
 psk="password"    
 priority=1 
 }
```
+
```sh
wpa_supplicant -iwlan0 -c /etc/wpa_supplicant/wpa_supplicant.conf
dhclient wlan0
```

. 使用图形界面设置 Wi-Fi 客户端
+
image::/picture/m4_wifi_iperf3.jpg[m4_wifi_iperf3.jpg]
