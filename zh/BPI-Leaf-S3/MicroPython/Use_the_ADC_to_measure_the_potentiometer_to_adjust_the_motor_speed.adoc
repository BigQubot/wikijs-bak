TIP: link:../GettingStarted_BPI-Leaf-S3[返回 BPI-Leaf-S3 入门指南]

== 外部硬件要求

* 电位器×1
* TB6612FNG 电机驱动模块 × 1
* 5 V 直流电机 × 1
* 若干连接线

== 连接方式

[options="header"]
|============================
| 电位器 | BPI-Leaf-S3
| GND           | GND        
| VCC           | 3V3        
| S             | 14         
|============================

[options="header"]
|========================
| TB6612FNG | BPI-Leaf-S3
| PWMA      | 11         
| AIN2      | 13         
| AIN1      | 12         
| STBY      | 10         
| VM        | 5V         
| VCC       | 3.3V       
| GND       | GND        
|========================

[options="header"]
|=========================
| TB6612FNG | 电机       
| AO1       | 电机 N 极
| AO2       | 电机 S 极
|=========================

== 运行结果

开发板每隔 100 ms 在 REPL 中输出 ADC 读取的电压值（单位 mV），以及对应的 PWM 占空比。

转动电位器可改变其输出电压。电压越高，开发板输出的 PWM 占空比越大，电机转速也越快。

https://www.youtube.com/watch?v=2_UeeeOBJwo[YouTube 视频]

== 代码
```py
from machine import Pin,ADC,PWM
import time

adc14 = ADC(Pin(14),atten=ADC.ATTN_11DB)

PWM_A = PWM(Pin(11)) #Set PWM output pin
PWM_A.freq(20000) #Set PWM frequency
PWM_A. duty(0) #Set PWM duty cycle
AIN1 = Pin(12,Pin.OUT)
AIN2 = Pin(13,Pin.OUT)
STBY = Pin(10,Pin.OUT)
AIN1.on() #MOTOR forward
AIN2.off()
STBY.on() #When STBY pin is at high level, TB6612FNG starts.

while True:
     read_mv=adc14.read_uv()//1000
     if read_mv <= 3000:
         duty_set = int(1023/3000 * read_mv)
     else:
         duty_set = 1023
     PWM_A. duty(duty_set)
     Duty_cycle = int(duty_set/1023*100)
     print("ADC_read={0}mv,Duty_cycle={1}%".format(read_mv,Duty_cycle))
     time. sleep_ms(100)

```
