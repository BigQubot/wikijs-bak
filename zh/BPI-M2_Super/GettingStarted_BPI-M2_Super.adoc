= 简介

TIP: 更多信息： link:/en/BPI-M2_Super/BananaPi_BPI-M2_Super[香蕉派 BPI-M2_Super]

== 规格参数


= 开发
== 准备

. 准备一个 usb-串行电缆，一个 5V/3A 适配器 type-c 电源。串口线用于控制台调试，type-c线用于 android 镜像下载和ADB调试。
. 准备一张至少8GB的SD卡 linux 开发, android 只支持 emmc 启动。
. 的 SOC ROM 第一启动媒体是 emmc，因此板无法从 SD 卡启动，如果 emmc 可以使用任何闪现的映像启动，更多信息请参阅板启动顺序。
. 仅 A311D 变体板有摄像头、mipi 面板和 npu 支持。

== Android
=== 准备

. 下载并安装 link:https://download.banana-pi.dev/d/3ebbfa04265d4dddb81b/files/?p=%2FTools%2Fimage_download_tools%2Faml_usb_burning_tool_V2_setup_v2.2.3.3.zip[反洗钱 Usb 烧录工具] 为了 android 通过type-c下载图片，仅支持 windows.
. 下载最新的 android 镜像，并确认 md5 校验和正确。

=== 安装镜像 Usb 烧录工具

. 打开USB_燃烧_Tool.exe，选择菜单File->Import image，选择 android 镜像文件 aml_upgrade_package.img.
+
image::/picture/m2s_android_install_1.png[m2s_android_install_1.png]
. 按住 USB 板上按钮，插件 type-c usb 电缆连接到 PC 或如果已连接电源适配器，请按 RST 按钮，大约两秒钟后，松开按钮，板将被正确识别。
+
image::/picture/m2s_android_install_3.png[m2s_android_install_3.png]
image::/picture/m2s_android_install_2.png[m2s_android_install_2.png]

. 点击下载工具的开始按钮，等待升级完成。
+
image::/picture/m2s_android_install_4.png[m2s_android_install_4.png]

. 烧录成功后，拔掉type-c usb 并连接电源适配器即可启动。
+
image::/picture/m2s_android_install_5.png[m2s_android_install_5.png]

. 单击“停止”按钮取消升级过程并关闭 USB 燃烧工具。

=== 使用 Aml Flash 工具安装映像
link:https://github.com/Dangku/aml-flash-tool[Aml 闪存工具] 是一个 linux 平台开源镜像 flash util Amlogic android.
```sh

 $ ./flash-tool.sh --img=/path/to/aml_upgrade_package.img --parts=all --wipe --soc=g12a --reset=y
```
image::/picture/m5_linux_flash.png[m5_linux_flash.png]

=== 构建 Android 源代码

. 获取 Android 9.0源代码
+
```sh
git clone https://github.com/BPI-SINOVOIP/BPI-A311D-Android9
```
或者您可以从以下位置获取源代码 tar 存档 link:https://pan.baidu.com/s/1rANGEB-1MLPCBXqOR5aYCg?pwd=8888[百度盘(密码：8888)] 或 link:https://drive.google.com/drive/folders/1INIABp_MbB5UcwfqujTngGLOZN7YGuWp?usp=share_link[谷歌云端硬盘]

. 构建 Android 9.0 源代码 +
请阅读源代码 link:https://github.com/BPI-SINOVOIP/BPI-A311D-Android9/blob/master/README.md[README.md]

=== Android DTB 叠加
Bananapi M2S DTBO idx 值表，发布镜像中默认 idx 值为 0。

[options="header" cols="1,2,2" width="68%"]
|=====
3+|**Bananapi M2S DTBO idx值表**
| idx值 | 设备树覆盖 | 描述    
| 0 | 安卓_p_覆盖 | 默认dtbo，没用                
| 1 | 无线网络_BT_rtl8822cs | 启用bpi rtl8822cs wifi/bt 模块 
| 2 | i2c1              | 启用 i2c 1                        
| 3 | i2c2              | 启用 i2c 2                        
| 4 | sdio              | 启用sdio                         
| 5 | 串口1             | 启用2个引脚 uart 1                
| 6 | 串口1_cts_实时传输     | 启用 4 个引脚 uart 1                
| 7 | 串口2             | 启用2个引脚 uart 2                
| 8 | 高保真_PCM5122      | 启用 i2s link:https://shumeipai.nxez.com/hifidac-hat-for-raspberry-pi[pcm5122 HiFi DAC]   
|=====

**如何应用新的 dtbo**

. 通过 sysfs 的 ADB 命令
+
```sh
 root@dangku-desktop:/tmp# adb root
 restarting adbd as root
 root@dangku-desktop:/tmp# adb remount
 remount succeeded
 root@dangku-desktop:/tmp# adb shell
 bananapi_m2s:/ # echo dtbo > /sys/class/unifykeys/name                                                
 bananapi_m2s:/ # echo "1" > /sys/class/unifykeys/write                                                
 bananapi_m2s:/ # reboot
```
. Uart 通过 sysfs 的控制台命令
+
```sh
 console:/ $ 
 console:/ $ su
 console:/ # echo dtbo > /sys/class/unifykeys/name                              
 [  115.702781@0] unifykey: name_store() 1302, name dtbo, 4
 [  115.702856@0] unifykey: name_store() 1311
 console:/ #
 console:/ # echo "1" > /sys/class/unifykeys/write                              
 [  129.262659@0] unifykey: write_store()  is a string
 [  129.262733@0] unifykey: dtbo, 1, 1
 [  129.265312@0] unifykey: amlkey_write 393
 [  129.292347@1] emmc_key_write:149, write ok
 console:/ # 
 console:/ # reboot
```
. 设置应用程序(待办事项)
+
检查启动情况 uart 调试消息并确认实际加载了哪个 dtbo，这里“1”表示设置 idx=1申请wifi_BT_rtl8822cs dtbo。
+
```sh
 load dtb from 0x1000000 ......
       Amlogic multi-dtb tool
       Single dtb detected
 find 2 dtbos
 dtbos to be applied: 1
 Apply dtbo 1
```
Unifykeys 存储在特定的 emmc 部分，在 USB 中选择“正常擦除”_燃烧_工具不会在下次更新时删除此数据，如果您希望在镜像下载后应用默认的 dtbo idx，则必须选择“全部删除”。
+
image::/picture/m2s_android_erase_all.png[m2s_android_erase_all.png]

**构建 Android 具有特定 DTBO 默认值的镜像。**

. 默认内置覆盖层定义在 device/bananapi/bananapi_m2s/Kernel.mk，您可以在此处添加新的覆盖 dtbo。
+
```sh
 DTBO_DEVICETREE := android_p_overlay wifi_bt_rtl8822cs i2c1 i2c2 sdio uart1 uart1_cts_rts uart2 hifi_pcm5122
```
. 默认应用 DTBO idx 定义在 device/bananapi/bananapi_m2s/BoardConfig.mk，您可以更改 idx 值来设置默认应用哪个覆盖 dtbo。
+
```sh
 BOARD_KERNEL_CMDLINE += androidboot.dtbo_idx=0
```
. DTS 文件位于 common/arch/arm64/boot/dts/amlogic/overlay/bananapi_m2s/ +
更多信息关于 android 设备树覆盖，请参考 link:https://source.android.com/devices/architecture/dto[谷歌 android 官方网站]

=== 安装 OpenGapps
. 从 OpenGapps 下载安装包， Android 发布镜像是 arm/android 9.0 版本。
+
image::/picture/opengapps.png[opengapps.png]

. 下载 link:https://download.banana-pi.dev/d/ca025d76afd448aabc63/files/?p=%2FTools%2Fapps%2Fdevice_id_v1.3.2.apk[装置_id.apk].
. 将 OpenGapp 包复制到 udisk 或 sdcard 根目录。
. 创建一个txt文件，名称为 **工厂_更新_参数aml** 在 udisk 或 sdcard 根目录中包含以下内容 android 恢复参数内容，并将文件名替换为实际下载的包。 +
优盘：
+
```sh
 --wipe_cache
 --update_package=/udisk/open_gapps-arm-9.0-pico-20210327.zip
```
SD卡：
+
```sh
 --wipe_cache
 --update_package=/sdcard/open_gapps-arm-9.0-pico-20210327.zip
```
. 将 udisk 或 sdcard 插入开发板并开机。
. OpenGapps 安装并认证。
+
Youtube 视频： https://youtu.be/fXOKmWfpqF8
+
您还可以在以下位置观看此视频link:https://www.bilibili.com/video/BV13y4y1s77i/[哔哩哔哩]

=== 开关Mipi面板
默认 android 发布映像仅支持一种 mipi 面板，因为硬件在启动时没有检测不同面板的逻辑，因此 [800x1280 bpi 面板] 默认启用，但您可以更改为 [1200x1920 bpi 面板] 默认设置为“设置”->“面板输出”

image::/picture/m2s_panel_switch.png[m2s_panel_switch.png]

=== 面板旋转
两个 10" mipi 面板都是纵向硬件显示，所以默认 android 发布镜像是纵向模式，但您可以将其旋转到 90/180/270 有两种方式。

. 设置->显示->屏幕旋转中的UI旋转
+
image::/picture/m2s-rotation.png[m2s-rotation.png]

. SurfaceFlinger旋转，需要 link:/en/BPI-M2_Super/GettingStarted_BPI-M2_Super#_build_android_source_code[修改 android 源代码] 并建立
+
更改默认的 sf 旋转属性
+
```sh
   diff --git a/device/bananapi/bananapi_m2s/bananapi_m2s.mk b/device/bananapi/bananapi_m2s/bananapi_m2s.mk
   index 1f51703..d592a44 100644
   --- a/device/bananapi/bananapi_m2s/bananapi_m2s.mk
   +++ b/device/bananapi/bananapi_m2s/bananapi_m2s.mk
   @@ -579,6 +579,6 @@ PRODUCT_PROPERTY_OVERRIDES += \
    else
    PRODUCT_PROPERTY_OVERRIDES += \
        ro.sf.lcd_density=213 \
    -    ro.sf.primary_display_orientation=0
   +    ro.sf.primary_display_orientation=90
    endif
```
更改 dts 中的触摸面板旋转
+
```sh
   diff --git a/common/arch/arm64/boot/dts/amlogic/bananapi_m2s.dts b/common/arch/arm64/boot/dts/amlogic/bananapi_m2s.dts
   index 4a698b0..3d41b63 100755
   --- a/common/arch/arm64/boot/dts/amlogic/bananapi_m2s.dts
   +++ b/common/arch/arm64/boot/dts/amlogic/bananapi_m2s.dts
   @@ -876,8 +876,8 @@
                   reg = <0x5d>;
                   reset-gpio = <&gpio GPIOA_6 GPIO_ACTIVE_HIGH>;
                   irq-gpio = <&gpio GPIOA_5 GPIO_ACTIVE_HIGH>;
   -               rotation = <4>; /* sf_rotation 0 */
   -               //rotation = <0>; /* sf_rotation 90*/
  +               //rotation = <4>; /* sf_rotation 0 */
  +               rotation = <0>; /* sf_rotation 90*/
                   //rotation = <5>; /* sf_rotation 180 */
                   //rotation = <3>; /* sf_rotation 270 */
```                   
                   
=== 定制 Android 开机标志
Android bootloader限制启动标志fb显示大小是 1080p60hz/1920x1080 默认值，以及 android 内核 dtb 分区表将引导徽标分区大小默认限制为 16MB。

. 准备一个16位bmp文件，命名为boot-logo.bmp
. 将 bmp 文件压缩为 boot-logo.bmp.gz
+
```sh
 $ gzip boot-logo.bmp
```
. 下载 link:https://download.banana-pi.dev/d/ca025d76afd448aabc63/files/?p=%2FTools%2Flogo_create_tools%2Fm2s_android_bootlogo_tool.zip[m2s_android_bootlogo_tool.zip]
. 提取此工具
+
```sh
 $ unzip m2s_android_bootlogo_tool.zip
 $ cd m2s_android_bootlogo_tool/
 $ ls -l logo/
 -rwxr--r-- 1 dangku dangku 525054 Sep 25 16:54 bootup.bmp
 -rwxr--r-- 1 dangku dangku 525054 Sep 25 16:54 bootup_secondary.bmp
 -rwxr--r-- 1 dangku dangku    184 May 19  2020 upgrade_bar.bmp
 -rwxr--r-- 1 dangku dangku 180072 May 19  2020 upgrade_error.bmp
 -rwxr--r-- 1 dangku dangku 180072 May 19  2020 upgrade_fail.bmp
 -rwxr--r-- 1 dangku dangku 180072 May 19  2020 upgrade_logo.bmp
 -rwxr--r-- 1 dangku dangku 180072 May 19  2020 upgrade_success.bmp
 -rwxr--r-- 1 dangku dangku    184 May 19  2020 upgrade_unfocus.bmp
 -rwxr--r-- 1 dangku dangku 180072 May 19  2020 upgrade_upgrading.bmp
```
. 复制 boot-logo.bmp.gz
+
```sh
 $ cp boot-logo.bmp.gz logo/bootup.bmp
 $ cp boot-logo.bmp.gz logo/bootup_secondary.bmp
```
. 创建目标 logo.img 使用img pack工具，m2s的二进制和相关库_安卓_开机标志_工具是复制自 <android-source-dir>/out/host/linux-x86
+
```sh
 $ ./logo_img_packer -r logo logo.img
```
. 带有 fastboot 的 Flash 启动徽标
+
```sh
 $ adb root
 $ adb remount
 $ adb reboot fastboot
```
等待几秒，检查fastboot是否连接
+
```sh
 $ fastboot device
 1234567890      fastboot
 $ fastboot flashing unlock_critical
 $ fastboot flashing unlock
 $ fastboot flash logo logo.img
 $ fastboot reboot                   
```                   

== Linux
=== 准备

. Linux 镜像支持SD卡或 EMMC 启动。
. 建议使用A1级卡，至少8GB。
. 确保可启动 EMMC 如果您想从 SD 卡启动，请格式化
. 确保 SD 卡已格式化 Linux 如果您想从以下位置启动，镜像会闪烁 EMMC 并使用Sd卡作为存储。
. 安装 bpi-tools 在你的 Linux 电脑(如果使用其他工具刷入镜像，请忽略此步骤)。如果您无法访问此 URL 或出现任何其他安装问题，请转至 link:https://github.com/bpi-sinovoip/bpi-tools[bpi-tools] 源存储库，手动下载并安装此工具。
+
```sh
 $ apt-get install pv
 $ curl -sL https://github.com/BPI-SINOVOIP/bpi-tools/raw/master/bpi-tools | sudo -E bash
```
. 下载 Linux 最新的 Linux 镜像，并确认md5校验和正确。
. 默认登录： pi/bananapi 或 root/bananapi
. 维基指南仅适用于 bananapi 4.9 桶 ubuntu/debian 镜像。

=== 将映像安装到 SD 卡
> 使用 Balena Etcher 安装映像 Windows, Linux 和 MacOS.

link:https://balena.io/etcher[巴莱纳蚀刻机] 是 Balena 的开源 GUI 闪存工具，可将操作系统映像闪存到 SD 卡或 USB 开车。

单击“Flash from file”以选择镜像。点击“选择目标”进行选择 USB 设备。点击“闪光！”开始燃烧。

image::/picture/etcher.jpg[etcher.jpg]

> 使用 Balena Cli 安装镜像 Windows, Linux 和 MacOS.

link:https://github.com/balena-io/balena-cli[巴莱纳 CLI] 是 balenaCloud 或 openBalena 的命令行界面。可以用来闪光 linux 镜像。从以下位置下载安装程序或独立包 link:https://github.com/balena-io/balena-cli/releases[巴莱纳-io] 和 link:https://github.com/balena-io/balena-cli/blob/master/INSTALL.md[安装 ]正确连接到您的 PC，然后您就可以使用“link:https://docs.balena.io/reference/balena-cli/#local-flash-image[本地闪存]" balena 的命令选项闪现 linux 镜像到 SD 卡或usb 开车。
```sh
 $ sudo balena local flash path/to/xxx-bpi-m2s-xxx.img.zip
 $ sudo balena local flash path/to/xxx-bpi-m2s-xxx.img.zip --drive /dev/disk2
 $ sudo balena local flash path/to/xxx-bpi-m2s-xxx.img.zip --drive /dev/disk2 --yes
```

> 使用 dd 命令安装镜像 Linux

挂载SD卡设备 /dev/sdX 如果自动安装则分区。其实 bpi-copy 与此 dd 命令相同。
```sh
 $ sudo apt-get install pv unzip
 $ sudo unzip -p xxx-bpi-m2s-xxx.img.zip | pv | dd of=/dev/sdX bs=10M status=noxfer
```
> 安装镜像 bpi-tools 上 Linux 

将 SD 卡插入 Linux 电脑并运行
```sh
 $ sudo apt-get install pv unzip
 $ sudo bpi-copy xxx-bpi-m2s-xxx.img.zip /dev/sdX
```

=== 安装镜像到 eMMC

. 准备一张SD卡 Linux 镜像闪存和启动板与此 SD 卡。
. 复制 Linux 镜像到udisk，将udisk插入主板并安装。
. 有两种安装方式linux镜像到板上。
- 使用 dd 命令安装，如果自动安装，则 umount mmcblk0p1 和 mmcblk0p2 分区。其实 bpi-copy 与此 dd 命令相同。
+
```sh
 $ sudo apt-get install pv unzip
 $ sudo unzip -p xxx-bpi-m2s-xxx.img.zip | pv | dd of=/dev/mmcblk0 bs=10M status=noxfer
```
- 安装 linux 乌磁盘中的镜像 bpi-tools 命令
+
```sh
 $ sudo apt-get install pv unzip
 $ sudo bpi-copy xxx-bpi-m2s-xxx.img.zip /dev/mmcblk0
```
. 下载完成后，安全关闭电源并弹出 SD 卡。

=== 构建 Linux 源代码
. 获取 Linux .bsp 源代码
+
```sh
 $  git clone https://github.com/BPI-SINOVOIP/BPI-M2S-bsp
```
. 构建bsp源代码 +
请阅读源代码 link:https://github.com/BPI-SINOVOIP/BPI-M2S-bsp/blob/master/README.md[README.md]
. 如果你想单独构建uboot和内核，请下载 link:https://github.com/Dangku/amlogic-u-boot/tree/khadas-g12b-v2015.01-m2s[u启动] 的 link:https://github.com/Dangku/amlogic-linux/tree/khadas-g12b-4.9.y-m2s[内核] 仅从以下位置获取工具链、启动脚本和其他配置文件 link:https://github.com/BPI-SINOVOIP/BPI-M2S-bsp/tree/master/aml-pack/g12b/bpi-m2s/linux[BPI-M2S-bsp]

=== DTB 叠加
. DTB 覆盖用于 40pin gpios 多功能配置并安装在 vfat 引导分区中，您可以使用 mount 命令检查挂载点。
+
```sh
 root@bananapi:~# ls /boot/overlays/
 custom_ir.dtbo      pwm_b-backlight.dtbo  spi0.dtbo
 ds3231.dtbo         pwm_c-beeper.dtbo     uart1_cts_rts.dtbo
 hifi_pcm5102a.dtbo  pwm_cd-c.dtbo         uart1.dtbo
 hifi_pcm5122.dtbo   pwm_cd.dtbo           uart2.dtbo
 i2c1.dtbo           pwm_ef.dtbo           waveshare_tft24_lcd.dtbo
 i2c2.dtbo           pwm_ef-f.dtbo         waveshare_tft35c_lcd.dtbo
 pwm_ab.dtbo         sdio.dtbo             waveshare_tft35c_rtp.dtbo
```
. 更新 vfat 中的覆盖环境 /boot/env.txt 启用您想要的功能。
+
```sh
 # Device Tree Overlays
 #   uart1           -- Enable UART1 (uart_A, GPIO Header PIN8 & PIN10)
 #   pwm_c           -- Enable PWM_C (GPIO Header PIN7)
 #   i2c2            -- Enable i2c2 (GPIO Header PIN3 & PIN5)
 #   spi0            -- Enable SPI0 (GPIO Header PIN19 & PIN21 & PIN23 & PIN24)
 overlays="i2c2 spi0 uart1"
```
. 必须重新启动板才能加载覆盖 dtb。

=== 启用相机
的 linux 发布镜像默认是关闭摄像头的，按照如下配置，可以自行开启。

. 更新 link:/en/BPI-M2_Super/GettingStarted_BPI-M2_Super#_dtb_overlay[DTB 覆盖] 环境在 /boot/env.txt 启用相机 dtbo。
+
```sh
 overlays="os08a10"
```
. 添加摄像头模块/etc/modules
+
```sh
 iv009_isp_iq
 iv009_isp_lens
 iv009_isp_sensor
 iv009_isp
```
. 创建并添加相机模块选项 /etc/modprobe.d/os08a10.conf
+
```sh
 #choose camera calibration parameters
 options iv009_isp_iq cali_name=0
 #choose isp register sequence
 options iv009_isp_sensor isp_seq_num=0
``` 
. 启用相机 isp systemd 服务
+
```sh
 $ sudo systemctl enable camera_isp_3a_server.service
```
摄像头设备是 /dev/video0 重新启动后。

=== 开关Mipi面板
默认 linux 发布映像仅支持一种 mipi 面板，因为硬件在启动时没有检测不同面板的逻辑，因此默认启用 800x1280 bpi 面板，但您可以更改为 [1200x1920 bpi 面板] 作为默认值 /boot/lcd_env.txt
```sh
 # Mipi panel type
 #    Symbol    | Resolution
 # ----------------------+-------------
 #    "lcd_0"   | 10" 800x1280 panel
 #    "lcd_1"   | 10" 1200x1920 panel
 panel_type=lcd_0
```
WARNING: 注意：双屏显示不工作 linux，所以使用mipi时请断开hdmi线。

=== 面板旋转
两个 10" mipi 面板都是纵向硬件显示，因此默认发布图像是纵向模式，但您可以将其旋转到 90/180/270. +
对于桌面映像，创建 xorg 配置文件 /usr/share/X11/xorg.conf.d/10-fbdev-rotate.conf 内容：
```sh
 Section "Device"
     Identifier "Configured Video Device"
     # Rotate off
 #   Option "Rotate" "off"
     # Rotate Right / clockwise, 90 degrees
     Option "Rotate" "CW"
     # Rotate upside down, 180 degrees
 #   Option "Rotate" "UD"
     # Rotate counter clockwise, 270 degrees
 #   Option "Rotate" "CCW"
 
 EndSection
 
 Section "InputClass"
     Identifier "Coordinate Transformation Matrix"
     MatchIsTouchscreen "on"
     MatchProduct "goodix-ts"
     MatchDevicePath "/dev/input/event0"
     MatchDriver "libinput"
     # Rotate Right / clockwise, 90 degrees 
     Option "CalibrationMatrix" "0 1 0 -1 0 1 0 0 1"
     # Rotate upside down, 180 degrees
 #   Option "CalibrationMatrix" "-1 0 1 0 -1 1 0 0 1"
     # otate counter clockwise, 270 degrees 
 #   Option "CalibrationMatrix" "0 -1 1 1 0 0 0 0 1"
 
 EndSection
```
对于服务器图像，您可以通过两种方式更改帧缓冲区旋转：

. Sysfs 动态变化。
+
```sh
 echo 0 > /sys/class/graphics/fbcon/rotate    //origin 0 degree
 echo 1 > /sys/class/graphics/fbcon/rotate    //90 degree
 echo 2 > /sys/class/graphics/fbcon/rotate    //180 degree
 echo 3 > /sys/class/graphics/fbcon/rotate    //270 degree
```
. 引导配置更改。 +
改变脸书_旋转环境 /boot/env.txt
+
```sh
 # Framebuffer Rotate
 # 0 - origin 0 degree
 # 1 - 90 degree
 # 2 - 180 degree
 # 3 - 270 degree
 fb_rotate=0
```

=== 接线Pi

WARNING: 注意：此WiringPi仅支持设置40pin gpio 输出、输入、 pwm 或软件 pwm，对于 io 函数为 i2c, spi，...，您必须启用 dtb 覆盖 boot.ini

. 构建并安装wiringPi，用于 debian，你必须 link:/en/BPI-M2_Super/GettingStarted_BPI-M2_Super#_enable_sudo_for_debian[安装须藤] 构建之前
+
```sh
 $ sudo apt-get update
 $ sudo apt-get install build-essential git
 $ git clone https://github.com/Dangku/amlogic-wiringPi
 $ cd amlogic-wiringPi
 $ chmod a+x build
 $ sudo ./build
```
. 运行 gpio readall 显示所有 40 针状态。
+
image::/picture/m2s_wiringpi.png[m2s_wiringpi.png]

. BPI GPIO 扩展板和示例 link:https://github.com/Dangku/amlogic-wiringPi/tree/master/examples[amlogic-wiringPi/examples] +
blinkall，闪烁所有针头 gpios，无扩展板。 +
lcd-bpi， link:https://docs.banana-pi.org/en/BPI_LCD_1602_display_module[BPI LCD 1602显示模块] 示例。 +
52pi-bpi， link:https://docs.banana-pi.org/en/BPI_OLED_Display_Module[BPI OLED 显示模块] 示例。 +
矩阵bpi， link:https://docs.banana-pi.org/en/BPI_RGB_LED_Matrix_Expansion_Module[BPI RGB LED 矩阵扩展模块] 示例。 +
berryclip-bpi， link:https://docs.banana-pi.org/en/BPI_BerryClip_Module[BPI BerryClip 模块]

=== RPi。GPIO
构建并安装，用于 debian，你必须 link:/en/BPI-M2_Super/GettingStarted_BPI-M2_Super#_enable_sudo_for_debian[安装须藤] 构建之前
```sh
 $ sudo apt-get update
 $ sudo apt-get install build-essential python3 python3-pip python3-dev python3-setuptools git
 $ git clone https://github.com/Dangku/RPi.GPIO-Amlogic.git
 $ cd RPi.GPIO-Amlogic
 $ sudo python3 setup.py clean --all
 $ sudo python3 setup.py install
```
创建并安装wheel包
```sh
 $ sudo python3 setup.py bdist_wheel
 $ sudo pip3 install dist/RPi.GPIO-XXX.whl
```
直接从git源码安装，无需开发
```sh
 $ sudo pip3 install git+https://github.com/Dangku/RPi.GPIO-Amlogic.git
```
如果该软件包已安装，则应在安装新软件包之前将其卸载，或者使用 --force-reinstall 选项安装新软件包。

=== 接线Pi2-Python
构建并安装，用于 debian，你必须 link:/en/BPI-M2_Super/GettingStarted_BPI-M2_Super#_enable_sudo_for_debian[安装须藤] 构建之前
```sh
 $ sudo apt-get update
 $ sudo apt-get install build-essential python3 python3-dev python3-setuptools swig git
 $ git clone --recursive  https://github.com/Dangku/WiringPi2-Python-Amlogic.git
 $ cd WiringPi2-Python-Amlogic
 $ sudo python3 setup.py install
```

=== 亮度示例
link:https://github.com/rm-hull/luma.examples[Luma.examples] 使用 GPIO.BCM gpio 默认模式，因此您应该将 40pin 接头引脚映射到 bcm gpio 编号并正确连接硬件。

. 构建并安装 RPi。GPIO
建造 bananapi m2s RPi。GPIO 用 python3 代替 python 因为luma使用的是python3。
+
```sh
 $ sudo apt-get update
 $ sudo apt-get install build-essential python3 python3-dev python3-setuptools git
 $ git clone https://github.com/Dangku/RPi.GPIO-Amlogic.git
 $ cd RPi.GPIO-Amlogic
 $ sudo python3 setup.py clean --all
 $ sudo python3 setup.py install
```
您可以更改 bcmledpin 变量 test/led.py 到你的硬件背光 gpio 并运行它来检查 RPi。GPIO 效果很好。
+
```sh
 $ sudo python3 test/led.py
```
硬件背光将重复打开和关闭
. luma.examples 库安装
+
```sh
 $ sudo usermod -a -G i2c,spi,gpio pi
```
如果组不存在，以下命令将创建它：
+
```sh
 $ sudo groupadd --system xxx
 $ sudo apt-get install python3-dev python3-pip libfreetype6-dev libjpeg-dev build-essential
 $ sudo apt-get install libsdl-dev libportmidi-dev libsdl-ttf2.0-dev libsdl-mixer1.2-dev libsdl-image1.2-dev
 $ git clone https://github.com/rm-hull/luma.examples.git
 $ cd luma.examples
```
安装luma.core、luma.emulator、luma。lcd，luma.le-matrix，亮度。oled pip libs，请确保此步骤没有错误或下载中断，如果出现错误，请重试
+
```sh
 $ sudo -H pip install -e .
```
或
+
```sh
 $ sudo -H pip3 install -e .
```
为了 debian 巴斯特(python 3.7) 其中不包括 /usr/bin/pip 在 python3-pip 包中，使用 pip3 安装 luma 包时会出现以下错误
+
```sh
 ...
 WARNING, No "Setup" File Exists, Running "buildconfig/config.py"
 Using UNIX configuration...
 
 /bin/sh: 1: sdl2-config: not found
 /bin/sh: 1: sdl2-config: not found
 /bin/sh: 1: sdl2-config: not found
 ...
```
安装 sdl2 相关软件包来解决此问题，然后使用 pip3 再次安装 luma libs
+
```sh
 $ sudo apt-get install libsdl2-dev libsdl2-ttf-dev libsdl2-mixer-dev libsdl2-image-dev
 $ sudo -H pip3 install -e .
check installed luma pip libs
 $ pip3 list | grep luma
   
   luma.core          2.4.0
   luma.emulator      1.4.0
   luma.lcd           2.10.0
   luma.led-matrix    1.7.0
   luma.oled          3.11.0
```
. 例子测试 +
启用 i2c 或 spi link:/en/BPI-M2_Super/GettingStarted_BPI-M2_Super#_dtb_overlay[覆盖层] 在运行测试示例之前
+
```sh
 $ cd examples
 $ sudo python3 bounce.py --config ../conf/ili9341.conf
```

=== HDMI LCD
[options="header" cols="1,1"]
|=====
2+| **Bananapi M2 超级测试 HDMI LCD**
| 面板                             | /boot/boot.ini                    
| link:https://www.waveshare.com/3.5inch-hdmi-lcd.htm[微享 3.5寸 480x320]                 | 设置环境显示_自动检测“假” +
setenv hdmimode "480x320p60hz" 
| link:https://www.waveshare.com/3.5inch-hdmi-lcd-e.htm[微享 3.5寸 640x480]                 |                                   
| link:https://www.waveshare.com/4inch-hdmi-lcd-c.htm[威享4寸720x720]                 |                                   
| link:https://www.waveshare.com/5inch-hdmi-amoled.htm[威享5寸960x544]                 |                                   
| link:https://www.waveshare.com/5inch-hdmi-lcd-h.htm[威享5寸800x480]                 |                                   
| link:https://www.waveshare.com/5.5inch-1440x2560-lcd.htm[微享 5.5寸 1440x2560]               | 设置环境显示_自动检测“假” +
setenv hdmimode "1440x2560p60hz"
| link:https://www.waveshare.com/7inch-hdmi-lcd-b.htm[威享 7寸 800x480]                 |                                   
| link:https://www.waveshare.com/7inch-hdmi-lcd-c.htm[威享 7寸 1024x600]                |                                   
| link:https://www.waveshare.com/7.9inch-HDMI-LCD.htm[微享 7.9英寸 400x1280]                |                                   
| link:https://www.waveshare.com/8inch-1536x2048-LCD.htm[威享 8寸 1536x2048]               | 设置环境显示_自动检测“假” +
setenv hdmimode "1536x2048p60hz"
| link:https://www.waveshare.com/8.8inch-Side-Monitor.htm[微享 8.8寸 480x1920]                |                                   
| link:https://www.waveshare.com/9inch-2560x1600-Monitor.htm[威享9寸2560x1600]               | 设置环境显示_自动检测“假” +
setenv hdmimode "2560x1600p60hz"
| link:https://www.waveshare.com/10.1inch-HDMI-LCD-E.htm[微享 10.1英寸 1024x600]	               |
| link:https://www.waveshare.com/10.1inch-hdmi-lcd-b-with-case.htm[微享 10.1寸 1280x800]	      |
| link:https://www.waveshare.com/11.9inch-HDMI-LCD.htm[微享 11.9英寸 320x1480]	               |
| link:https://www.waveshare.com/12.3inch-hdmi-lcd.htm[微享 12.3英寸 1920x720]	               |
| link:https://www.waveshare.com/13.3inch-HDMI-LCD-H.htm[微享 13.3英寸 1920x1080]	             |
| link:https://www.waveshare.com/15.6inch-hdmi-lcd-h-with-case.htm[微享 15.6英寸 1920x1080]	    |
|=====
背光控制：https://github.com/Dangku/Waveshare-USB-Brightness

=== 定制 Linux 开机标志
Linux uboot 将启动徽标 fb 大小限制为 1080p60hz/1920x1080 默认情况下，默认镜像不支持超大分辨率，但可以修改uboot源代码以支持它。

. 准备一个24位bmp文件，命名为boot-logo.bmp
. 将目标文件复制到 /boot/firmware/ 或 /boot/ 目录。

== 其他开发
=== 启动顺序

image::/picture/m5_linux_boot_squence.png[m5_linux_boot_squence.png]

检查从 SD 卡加载的引导加载程序或 EMMC 在控制台调试消息的开头

. ROM 从 SD 卡加载引导加载程序 (Linux 日志示例)
+
```sh
 ...
 
 BL2 Built : 15:21:42, Mar 26 2020. g12a g486bc38 - gongwei.chen@droid11-sz
 
 Board ID = 1
 Set cpu clk to 24M
 Set clk81 to 24M
 Use GP1_pll as DSU clk.
 DSU clk: 1200 Mhz
 CPU clk: 1200 MHz
 Set clk81 to 166.6M
 board id: 1
 Load FIP HDR DDR from SD, src: 0x00010200, des: 0xfffd0000, size: 0x00004000, part: 0
 fw parse done
 PIEI prepare done
 fastboot data verify
 result: 255
 Cfg max: 12, cur: 1. Board id: 255. Force loop cfg
 DDR4 probe
 
 ...
```
. ROM 加载引导加载程序 EMMC(Android 日志示例)
+
```sh
 ...
 
 Board ID = 1
 Set cpu clk to 24M
 Set clk81 to 24M
 Use GP1_pll as DSU clk.
 DSU clk: 1200 Mhz
 CPU clk: 1200 MHz
 Set clk81 to 166.6M
 eMMC boot @ 0
 sw8 s
 board id: 1
 Load FIP HDR DDR from eMMC, src: 0x00010200, des: 0xfffd0000, size: 0x00004000, part: 0
 fw parse done
 PIEI prepare done
 00000000
 emmc switch 1 ok
 ddr saved addr:00016000
 Load ddr parameter from eMMC, src: 0x02c00000, des: 0xfffd0000, size: 0x00001000, part: 0
 00000000
 
 ...
``` 

=== 擦除 EMMC 用于 SD 卡启动
有四种可能的情况需要注意， EMMC 已经闪现了 Android 镜像， EMMC 已经闪现了 Linux 镜像、BL2 中的启动进程挂起以及 EMMC 空的。

. 可启动 EMMC 与 Android 镜像闪烁
- 使用 usb 烧录工具，拔掉type-c usb 下载过程中的电缆 **7% 格式化**
+
image::/picture/m5_android_format.png[m5_android_format.png]

- 使用 Android Fastboot 工具，确保 adb/fastboot 在执行此操作之前，工具已在您的 PC 上运行。
+
```sh
   root@dangku-desktop:/tmp# adb root
   adbd is already running as root
   root@dangku-desktop:/tmp# adb remount
   remount succeeded
   root@dangku-desktop:/tmp# adb shell
   bananapi_m2s:/ # reboot fastboot
```
等待几秒钟，主板重新启动至快速启动模式
+
```sh
   root@dangku-desktop:/tmp# fastboot devices
   1234567890	fastboot
   root@dangku-desktop:/tmp# fastboot flashing unlock_critical
   ...
   OKAY [  0.044s]
   finished. total time: 0.044s
   root@dangku-desktop:/tmp# fastboot flashing unlock
   ...
   OKAY [  0.047s]
   finished. total time: 0.047s
   root@dangku-desktop:/tmp# fastboot erase bootloader
   erasing 'bootloader'...
   OKAY [  0.059s]
   finished. total time: 0.059s
   root@dangku-desktop:/tmp# fastboot erase bootloader-boot0
   erasing 'bootloader-boot0'...
   OKAY [  0.036s]
   finished. total time: 0.036s
   root@dangku-desktop:/tmp# fastboot erase bootloader-boot1
   erasing 'bootloader-boot1'...
   OKAY [  0.035s]
   finished. total time: 0.035s
```
- 使用uboot命令，连接调试控制台电缆并在开机时按ESC键进入uboot命令行
+
```sh
   bananapi_m2s_v1#amlmmc erase 1
   emmckey_is_protected(): protect
   start = 0,end = 57343
   start = 221184,end = 30535679
   Erasing blocks 0 to 8192 @ boot0
   start = 0,end = 8191
   Erasing blocks 0 to 8192 @ boot1
   start = 0,end = 8191
   bananapi_m2s_v1#reset
   resetting ...
   SM1:BL:511f6b:81ca2f;FEAT:A0F83180:20282000;POC:F;RCY:0;EMMC:0;READ:0;CHK:1F;READ:0;CHK:1F;READ:0;CHK;
```
这两种方式实际上是擦除了bootloader部分 EMMC android, 从 SD 卡启动后 Linux, 你最好 link:/en/BPI-M2_Super/GettingStarted_BPI-M2_Super#_erase_emmc_android_by_dd_command[格式化整体 EMMC 通过 dd 命令].

- 最简单的方法是插入 SD 卡 Linux 开机前镜像闪烁， Android 引导加载程序将检查 boot.ini 文件是否存在于SD卡vfat分区，以便SD卡 Linux 将启动。启动后，您可以 link:/en/BPI-M2_Super/GettingStarted_BPI-M2_Super#_erase_emmc_android_by_dd_command[格式化整体 EMMC 通过 dd 命令] 然后闪烁 Linux 镜像到 EMMC.
+
```sh
   ...
   BPI: try boot from sdcard
   reading boot.ini
   2453 bytes read in 3 ms (797.9 KiB/s)
   ## Executing script at 03080000
   Starting boot.ini...
   reading env.txt
   3483 bytes read in 7 ms (485.4 KiB/s)
   HDMI: Autodetect: 1080p60hz
   reading Image.gz
   10924573 bytes read in 611 ms (17.1 MiB/s)
   reading bananapi_m2s.dtb
   88054 bytes read in 12 ms (7 MiB/s)
   reading uInitrd
   11704481 bytes read in 655 ms (17 MiB/s)
   reading overlays/wifi_bt_rtl8822cs.dtbo
   729 bytes read in 6 ms (118.2 KiB/s)
```
. 可启动 EMMC 与 Linux 镜像闪烁
- 使用uboot命令，连接调试控制台电缆并在开机时按ESC键进入uboot命令行
+
```sh
   bananapi_m2s# mmc erase 0 1000
```
- Linux u-boot 还检查 boot.ini 文件是否存在于SD卡vfat分区中，以便SD卡 Linux 将启动。开机后可以格式化整个 EMMC 通过 dd 命令或 flash Linux 镜像直接到 EMMC.

. A **极端情况** bootloader 或 uboot 是否损坏，ROM 从中加载 EMMC 但在u-boot或BL2中挂起，例如启动过程会在BL2中挂起 EMMC 如果 dram 初始化失败，唯一的方法是格式化 EMMC 与 usb 烧录工具，或下载 Android 镜像完全，然后尝试其他方法擦除 EMMC 或闪光 Linux 镜像到 EMMC.

. 如果出现以下情况，Rom 将尝试直接从 SD 卡加载引导加载程序 EMMC 是空的。

=== 擦除 Emmc Android 通过 dd 命令
如果板子闪了 android 之前，整个 emmc 如果你想用SD卡启动它，必须通过这些命令擦除 Linux 镜像。
```sh
 $ sudo dd if=/dev/zero of=/dev/mmcblk0boot0 bs=1M status=noxfer 
 $ sudo dd if=/dev/zero of=/dev/mmcblk0boot1 bs=1M status=noxfer
 $ sudo dd if=/dev/zero of=/dev/mmcblk0 bs=1M status=noxfer 
 $ sync
```
=== Wifi/BT 支持
. Android 测试和支持。
+
```sh
 rtl8723bu wifi/bt(usb)
 rtl8188eu wifi(usb)
 rtl8821cu wifi/bt(usb)
 rtl8822cs wifi/bt(sdio/uart)
 rtl8814au wifi(usb), please get the aircrack-ng driver and install.
```
**如何启用 Android Wifi/BT** +
USB 类型： 插入式 usb 加密狗到 usb 主机端口并重新启动系统，启动后，您可以在“设置”应用程序中启用或禁用 wifi 和蓝牙。 +
SDIO/UART 类型：将硬件模块正确连接到40针排座上并 link:/en/BPI-M2_Super/GettingStarted_BPI-M2_Super#_android_dtb_overlay[配置 Android DTB 叠加] 来启用它。
+
WARNING: 注意： Android 不支持那个 ethernet 和wifi同时连接， Ethernet 比wifi有更高的优先级，这意味着wifi无法连接网络，如果 ethernet 已经连接，如果出现以下情况，wifi 将断开连接 ethernet 电缆插件。

. Linux 测试和支持。
+
```sh
 rtl8188eu wifi(usb)
 rtl8192eu wifi(usb)
 rtl8723bu wifi/bt(usb)
 rtl8811au wifi(usb)
 rtl8812au wifi(usb)
 rtl8812bu wifi(usb)
 rtl8821cu wifi/bt(usb)
 rtl8822cs wifi/bt(sdio/uart)
```
+
--
**如何启用 Linux 无线网络**

Wifi 模块驱动程序已在发布映像中预构建。

USB 类型： 插入式 usb 加密狗到 usb 主机端口和驱动程序将自动加载。
SDIO/UART 类型：

- 将硬件模块正确连接到40针排座。
- 配置 link:/en/BPI-M2_Super/GettingStarted_BPI-M2_Super#_dtb_overlay[DTB 覆盖]
+
```sh
   overlays="wifi_bt_rtl8822cs"
```
- 添加wifi模块名称 /etc/modules 用于下次启动时自动加载。
+
```sh
 # This file contains the names of kernel modules that should be loaded
 # at boot time, one per line. Lines beginning with "#" are ignored.
 88x2cs
```
--
+
--
**如何启用 Linux 蓝牙**

- 请下载 link:https://github.com/Dangku/m5-rtk-linux-bt-driver[rtk-linux-bt-驱动程序] 源代码、构建和安装 usb 或 uart RTK linux 蓝牙 drivers/firmwares 到你的形象。
- 对于 USB 类型，插入 usb 加密狗到 usb 主机端口和驱动程序将自动加载。
- 对于 UART 类型，在安装蓝牙之前将 dtb 覆盖配置为与 wifi 相同 drivers/firmwares. 人机交互_当rtk-hciuart.service启动时，uart驱动将被加载。

--

=== 云初始化&Snap
Cloud-init 和 Snap 服务默认启用，您可以禁用或删除它们。

. 禁用或删除 cloud-init
+
```sh
 $ sudo touch /etc/cloud/cloud-init.disabled
```
或
+
```sh
 $ sudo apt purge cloud-init
```
. 禁用或删除快照
+
```sh
 $ sudo apt purge snapd
```

=== 启用 rc-local
systemd 服务 rc-local.service 已存在于发布映像中，但没有 [安装] 单元文件中的一部分。因此，Systemd 无法启用它。首先，我们必须更新文件。
```sh
 $ sudo nano /lib/systemd/system/rc-local.service
```
```sh
 [Unit]
 Description=/etc/rc.local Compatibility
 Documentation=man:systemd-rc-local-generator(8)
 ConditionFileIsExecutable=/etc/rc.local
 After=network.target
 
 [Service]
 Type=forking
 ExecStart=/etc/rc.local start
 TimeoutSec=0
 RemainAfterExit=yes
 GuessMainPID=no
 
 [Install]
 WantedBy=multi-user.target
 Alias=rc-local.service
```
创建 /etc/rc.local 文件。
```sh
 sudo nano /etc/rc.local
 #!/bin/sh
 #
 # rc.local
 #
 # This script is executed at the end of each multiuser runlevel.
 # Make sure that the script will "exit 0" on success or any other
 # value on error.
 #
 # In order to enable or disable this script just change the execution
 # bits.
 #
 # By default this script does nothing.
 
 exit 0
```
添加可执行权限 /etc/rc.local
```sh
 $ sudo chmod +x /etc/rc.local
```
启用 rc-local.service 并重新启动
```sh
 $ sudo systemctl enable rc-local.service
 $ sudo reboot
```

=== 启用 sudo Debian
发布 Debian image默认不安装sudo，使用“su -”命令，用户可以更改为root。如果你喜欢sudo，可以安装它。
```sh
 $ su root
 Password:(enter bananapi)
 
 # apt-get update
 # apt-get install sudo
 # adduser pi sudo
```
然后请注销并重新登录

=== 安装 Docker发动机
安装 Docker 发动机开启 Ubuntu 20.04 服务器

. 设置存储库
更新 apt 软件包索引并安装软件包以允许 apt 通过 HTTPS 使用存储库：
+
```sh
 $ sudo apt-get update
 $ sudo apt-get install apt-transport-https ca-certificates curl gnupg lsb-release
```
添加 Docker官方GPG密钥：
+
```sh
 $ curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
```
设置稳定存储库
+
```sh
 $  echo \
    "deb [arch=arm64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu \
    $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```
. 安装 Docker发动机
+
```sh
 $ sudo apt-get update
 $ sudo apt-get install docker-ce docker-ce-cli containerd.io
```
. 验证 Docker 通过运行 hello-world 映像正确安装引擎。
+
```sh
 $ sudo docker run hello-world
```
+
image::/picture/docker-test.png[docker-test.png]
+
安装 docker 用一个简单的命令
+
```sh
 $ curl -sSL get.docker.com | sudo sh
```
link:https://docs.docker.com/engine/install/[安装 Docker发动机] 在其他方面 Linux 分布

