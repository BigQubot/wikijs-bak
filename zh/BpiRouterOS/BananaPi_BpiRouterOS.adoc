# BpiRouter v1.5.0-rc1：功能与使用说明

 本文适用于 v1.5.0-rc1。这是供用户在正式版之前试用的发布候选版本。
用于重要网络之前，请备份设置并准备好重新安装的方法。
可用网口、Wi-Fi 频段和移动网络取决于板型及实际安装的硬件。

## 选择板型

提供 BPI-R4、BPI-R3、BPI-R4-Lite、BPI-R3-Mini、OpenWrt One、
BPI-R4-Pro 8X 和 BPI-R4-Pro 4E 的镜像。必须选择与实物型号完全一致的下载。
R4-Pro 4E 为实验性支持，不能使用 8X 镜像。4E 的 WAN 电口和 SFP 口
在启动时二选一，不能同时作为两个 WAN 使用；默认使用电口，插入网线或
光模块不会自动切换。本版本的 4E 不提供固件回退功能。

## 开始使用

安装完成后，将电脑连接到 LAN 口，打开 `https://192.168.100.1`，
按初始设置向导设置管理员密码和 Wi-Fi 密码。界面提供中文和英文、
浅色和深色主题，支持电脑和手机访问。

在“首页 → 系统信息”或“系统”页面顶部查看产品版本。
本版应显示 `BpiRouter v1.5.0-rc1`。单独列出的 OpenWrt 版本不是
BpiRouter 产品版本。如果显示“未知”，不要据此推测安装了哪个版本

## 各页面提供的功能

[options="header",cols="2,8"]
|====
| 页面 | 可以做什么 
| 首页 | 查看互联网连接、流量、已连接设备、Wi-Fi 和路由器资源使用情况。 
| 网络 | 设置上网连接、局域网地址、Wi-Fi、移动网络模组、USB 共享、多 WAN 和 WAN 带宽整形。 
| 设备 | 查看已连接设备、设置设备名称、允许或禁止设备访问。 
| 分段 | 设置独立的 Guest 访客和 IoT 物联网 Wi-Fi、访问规则及共享带宽上限。 
| 高级 | 设置 DNS 过滤、访问时段、端口转发、防火墙、VPN、动态域名，并使用网络诊断。 
| 系统 | 设置路由器名称、时间和密码；备份或恢复设置、管理固件、重启及恢复出厂设置。 
| 用户 | 管理管理员、操作员和只读账户，需要管理员权限。 

|====

### 上网和 Wi-Fi

在“网络”中选择上网连接，填写运营商要求的信息。有线上网支持自动获取
地址、固定地址和 PPPoE。IPv6 需要运营商和连接设备支持。
也可以使用兼容的移动网络模组，或已开启 USB 网络共享的手机。

编辑 Wi-Fi 可修改名称、密码和无线设置。只有板子实际配备的频段可用。
信道及 6 GHz 可用性受所在国家设置和终端支持情况影响。
修改 Wi-Fi 设置可能导致无线设备断开，建议通过网线操作。

移动网络在本版支持兼容的移远 QMI 和 NCM 模组，不支持 MBIM。
能否联网以及使用 4G 还是 5G，取决于模组、SIM 卡、天线、运营商及覆盖情况。
不支持 SIM 卡热拔插：更换内置模组的 SIM 卡前，先关闭路由器电源；
更换外置 USB Dongle 的 SIM 卡前，先拔出 Dongle，换好后重新接入。
不要在模组通电时反复只拔插 SIM 卡。

### 多条上网线路

在“网络 → 多 WAN”中选择参与的连接，并选择故障切换或负载均衡。
故障切换会在首选连接失效时改用备用连接；负载均衡将不同连接分配到
多条线路，不保证单次下载速度等于所有线路速度之和。

### 带宽控制

- “网络 → WAN SQM”：按需为每条 WAN 开启带宽整形，并填写下载和上传
  速率。可以选择常用值，也可以填写实际测得的线路速率；略低于可用速率
  的设置有助于减小网络繁忙时的延迟。默认关闭。在故障切换模式下，
  整形跟随当前使用的 WAN；不能与负载均衡同时启用。
- “分段 → Guest / IoT”：分别设置两个网络的上传和下载上限。
  上限由该分段所有设备共同分享，不是每台设备各有一份。
  某个方向填零表示该方向不限速。这些限制可以与多 WAN 负载均衡一起使用。
- 本版不提供按单台设备或按协议限速。

### 访客、物联网和设备访问

在“分段”中启用 Guest 或 IoT，选择 Wi-Fi 设备，填写网络名称和密码。
它们共享板上已有的 Wi-Fi 设备，无需额外购买无线网卡。
保存前检查互联网访问、局域网访问和设备隔离设置。
每个启用的分段至少需要一个 Wi-Fi 网络。

在“设备”中通过“允许”或“禁止”控制设备访问。
设备名称和地址只是管理线索，不能据此确认使用者身份。
访问时段和 DNS 过滤位于“高级”；DNS 过滤不能阻止所有应用或所有网站访问方式。

### VPN、动态域名和高级连接

管理员可以在“高级”中导入 WireGuard 或 OpenVPN 客户端配置以连接 VPN，
也可以设置 VPN 服务端并生成客户端配置。
从外部访问需要可达的互联网地址及适当的运营商和网络设置，
开启 VPN 服务端不能绕过运营商对外部接入的限制。

动态域名将域名关联到变化的互联网地址。选择 DuckDNS、No-IP、Dynu、
Cloudflare 或 FreeDNS，并填写相应服务商要求的凭据。
发起更新不表示服务商一定已接受；用于远程访问前，请确认域名已正确更新。
动态域名不能使原本无法从外部访问的连接变得可达。

设置端口转发或防火墙规则时，只开放确实需要对外提供的服务。
修改网络访问设置时，保留另一种访问路由器的方法。
如果出现确认倒计时，检查连接仍正常后再确认。

## 设置、固件与帮助

重要修改前，在“系统”下载设置备份。备份可能含密码和密钥，
应妥善保管，不要随求助信息发送。固件更新、恢复设置和恢复出厂设置
需要管理员权限。恢复出厂设置会清除个人设置。

安装 v1.5 时，使用对应的安装介质重新安装，不要从旧版或其他厂商固件
直接保留设置升级。请遵循[烧录手册](INSTALLATION.zh-CN.md)；
升级镜像不能代替首次安装介质。



产品使用问题请联系 [link:https://www.banana-pi.org/contact-us/[比派公司]]，
或前往[link:https://forum.banana-pi.org/[比派官方论坛]]，说明准确板型、产品版本、
操作步骤和界面提示。不要发送密码、令牌、私钥或 SIM 卡标识。

---
**link:/zh/BpiRouterOS/BananaPi_BpiRouterOS_Flashing[烧录指南]**、**link:/zh/BpiRouterOS/BananaPi_BpiRouterOS_Setup[首次配置]**、**link:/zh/BpiRouterOS/BananaPi_BpiRouterOS_Recovery[恢复与救砖]**